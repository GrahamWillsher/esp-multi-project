#ifndef HTTP_SSE_UTILS_H
#define HTTP_SSE_UTILS_H

// Backward-compatible shim: canonical implementation now lives in esp32common.
#include <webserver_common_utils/http_sse_utils.h>

#endif // HTTP_SSE_UTILS_H
