#pragma once

// LEGACY MODULE RETIRED.
//
// `display_core.*` previously hosted an alternate/legacy display path.
// Active code must include `display.h` and use the public display API.

#include "display.h"
